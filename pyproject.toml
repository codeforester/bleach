[project]
name = "bleach"
version = "0.1.0"
description = "Local offline PII redaction CLI for Base-managed workspaces."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openpyxl>=3.1.5",
    "pymupdf>=1.27.2.3",
]

[tool.uv]
package = false
